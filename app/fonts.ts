/**
 * Font roles are consumed only as the CSS variables --font-season-mix,
 * --font-acumin and --font-akkurat-mono, which app/globals.css maps onto the
 * --font-display / --font-sans / --font-mono roles. Until the licensed files
 * are added the var() fallbacks in globals.css supply system faces.
 *
 * See app/fonts/README.md for the files to add and the code to activate.
 */
export const fontVariables = "";
